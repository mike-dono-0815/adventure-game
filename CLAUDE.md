# Adventure Game

## Starting the game

The game requires a local HTTP server (JSON files are loaded via `fetch`).

```bash
python -m http.server 8080
```

Then open **http://localhost:8080** in your browser.
